global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: '1inch-rfq-metrics-server'
    static_configs:
      - targets: ['price-feeder:9091']  # Separate metrics server
    scrape_interval: 5s
    metrics_path: /metrics